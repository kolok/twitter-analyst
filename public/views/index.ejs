<!DOCTYPE html>
<html>
    <head>
        <% include ./partials/head %>
    </head>
    <body>
        <header>
            <% include ./partials/header %>
        </header>
        <main>
        <div class="container">
        <div class="jumbotron text-center wow fadeInUp">
            <h3>Enter a Search Term <small>(then hit enter)</small></h3>
            <form action="/results" style="margin-top: 25px;">
                <input type="text" class="form-control input-lg" placeholder="Search.." name="q" required />
            </form>
        </div>

        <% if(searches.length > 0){ %>
            <div class="searchSpace wow fadeInUp" data-wow-delay="1s">
            <h3>Latest Searches</h3>
            </div>
        <% } %>

        <% searches.forEach(function(query, index) { %>
            <div class="col-sm-6 searchSpace panel wow fadeInUp" data-wow-delay="1s">
                <a class="btn btn-block btn-primary btn-lg" href="/results?q=<%= query.term %>"><%= query.term %></a>
            </div>
        <% }) %>

        </div>
    </main>
        <footer class="page-footer white center-on-small-only">
            <% include ./partials/footer %>
        </footer>

    </body>
</html>
